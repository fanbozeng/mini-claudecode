[project]
name = "learnmcp"
version = "0.1.0"
description = "A hands-on Python MCP learning demo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.18.0",
]

[tool.uv]
package = false
